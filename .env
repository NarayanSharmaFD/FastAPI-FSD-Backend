# Local development using SQLite
DATABASE_URL=sqlite:///./tasktracker.db

# JWT
SECRET_KEY=dev-secret-key-for-local
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS (JSON array so pydantic can parse it)
BACKEND_CORS_ORIGINS=["http://localhost:3000"]

# Other
ENV=development
